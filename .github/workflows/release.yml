name: Release

on:
  push:
    tags: ["v*"]

jobs:
  release:
    uses: diaryx-org/.github/.github/workflows/reusable-plugin-release.yml@main
    with:
      plugin-id: diaryx.storage-gdrive
      crate-name: diaryx_storage_gdrive_extism
    secrets:
      REGISTRY_PAT: ${{ secrets.REGISTRY_PAT }}
